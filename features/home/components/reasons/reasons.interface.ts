export interface IReason {
  /** Card badge illustration — decorative, so it renders with an empty alt. */
  icon: string;
  title: string;
  description: string;
}

export interface IAvatar {
  /** Full shorthand background: the photo layered over a gradient that doubles as its loading tint. */
  background: string;
}
